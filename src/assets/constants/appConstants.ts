const appNavigationStates = {
  auth: 'auth',
  user: 'user',
};

const appThemes = {
  dark: 'dark',
  light: 'light',
  default: 'default',
};

const paginationConstant = {
  limit: 20,
};

export {appNavigationStates, appThemes, paginationConstant};
